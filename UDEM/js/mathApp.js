// mathApp.js

// Import the module
const math = require('./firstMath');

// Log the imported module to see its structure
console.log(math);

// Use the imported functions and constants
console.log(math.add(2, 3)); // Outputs 5
console.log(math.PI); // Outputs 3.14159
console.log(math.square(4)); // Outputs 16